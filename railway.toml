[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn main:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1 --worker-class sync"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
